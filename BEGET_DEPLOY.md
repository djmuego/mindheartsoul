# 🚀 Деплой на Beget Cloud

## 📋 Пошаговая инструкция

### Шаг 1: Подготовка файлов

✅ **Production build готов!**
- Файл: `mindheartsoul-dist.tar.gz` (118 KB)
- Содержит: готовый статический сайт из папки `dist/`

### Шаг 2: Загрузка на Beget Cloud

#### Вариант A: Через веб-интерфейс Beget

1. Войдите в панель управления: https://cp.beget.com/cloud
2. Выберите или создайте виртуальный хостинг
3. Откройте файловый менеджер
4. Перейдите в директорию `public_html` (или создайте поддиректорию, например `mindheartsoul`)
5. Загрузите архив `mindheartsoul-dist.tar.gz`
6. Распакуйте архив:
   ```bash
   tar -xzf mindheartsoul-dist.tar.gz
   mv dist/* ./
   rm -rf dist mindheartsoul-dist.tar.gz
   ```

#### Вариант B: Через SSH/FTP

**SSH:**
```bash
# Подключитесь к серверу
ssh your_user@your_server.beget.tech

# Перейдите в директорию сайта
cd public_html

# Загрузите архив (используйте scp с локальной машины)
# scp mindheartsoul-dist.tar.gz your_user@your_server.beget.tech:~/public_html/

# Распакуйте
tar -xzf mindheartsoul-dist.tar.gz
mv dist/* ./
rm -rf dist mindheartsoul-dist.tar.gz
```

**FTP:**
1. Подключитесь через FTP клиент (FileZilla, WinSCP и т.д.)
2. Загрузите содержимое папки `dist/` в `public_html/`

### Шаг 3: Настройка .htaccess для SPA

Создайте файл `.htaccess` в корне сайта (public_html):

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  
  # Если файл или директория существуют, отдаём их
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  
  # Все остальные запросы перенаправляем на index.html (для React Router)
  RewriteRule . /index.html [L]
</IfModule>

# Кэширование статических ресурсов
<IfModule mod_expires.c>
  ExpiresActive On
  
  # JavaScript и CSS
  ExpiresByType text/css "access plus 1 year"
  ExpiresByType application/javascript "access plus 1 year"
  
  # Изображения
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  
  # Шрифты
  ExpiresByType font/woff2 "access plus 1 year"
  ExpiresByType font/woff "access plus 1 year"
</IfModule>

# Gzip сжатие (если не включено по умолчанию)
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript
</IfModule>

# Безопасность
<IfModule mod_headers.c>
  # X-Frame-Options
  Header always set X-Frame-Options "SAMEORIGIN"
  
  # X-Content-Type-Options
  Header always set X-Content-Type-Options "nosniff"
  
  # X-XSS-Protection
  Header always set X-XSS-Protection "1; mode=block"
</IfModule>
```

### Шаг 4: Настройка переменных окружения (опционально)

Если вы хотите использовать реальный Gemini API:

1. **На сервере** переменные окружения не работают для статических сайтов
2. **Решение:** Используйте Beget Cloud Functions или Node.js хостинг для прокси API запросов
3. **Альтернатива:** Настройте API ключ через отдельный backend

**Примечание:** Для статического хостинга на Beget приложение будет работать в режиме "без AI ключа" с сообщением "AI temporarily unavailable".

### Шаг 5: Проверка деплоя

1. Откройте ваш домен в браузере: `http://your-domain.beget.tech/`
2. Проверьте основные страницы:
   - ✅ Главная страница (/)
   - ✅ Вход/Регистрация (/auth)
   - ✅ Профиль (/profile)
   - ✅ Настройки (/settings)
3. Проверьте React Router:
   - Попробуйте перейти по разным URL
   - Обновите страницу (F5) - должна открываться та же страница

### Шаг 6: Настройка SSL (опционально, но рекомендуется)

1. В панели Beget перейдите в раздел SSL
2. Получите бесплатный сертификат Let's Encrypt
3. Сайт будет доступен по HTTPS: `https://your-domain.beget.tech/`

---

## 📦 Структура файлов после деплоя

```
public_html/
├── .htaccess                    # Конфигурация Apache
├── index.html                   # Главный HTML файл
└── assets/                      # Статические ресурсы
    ├── index-CSYqhGgu.css      # Стили (37 KB)
    └── index-D2-tZnLn.js       # JavaScript bundle (426 KB)
```

---

## 🔧 Troubleshooting

### Проблема: 404 при обновлении страницы

**Решение:** Проверьте `.htaccess` - должны быть правила rewrite для SPA

### Проблема: Стили не применяются

**Решение:** Проверьте пути к ресурсам. В `index.html` должны быть правильные пути к `/assets/`

### Проблема: "Cannot GET /page"

**Решение:** Убедитесь, что mod_rewrite включен на сервере и `.htaccess` работает

### Проблема: AI не работает

**Ожидаемо:** На статическом хостинге AI будет показывать сообщение "temporarily unavailable". Это нормально, так как `VITE_GEMINI_API_KEY` вшивается в build time.

**Решение для production:**
1. Создайте backend API endpoint (Node.js/PHP) на отдельном хостинге
2. Проксируйте запросы к Gemini через ваш backend
3. Храните API ключ только на backend

---

## 🎯 Быстрый деплой (копипаста для SSH)

```bash
# 1. Загрузите архив на сервер (с локальной машины)
scp mindheartsoul-dist.tar.gz your_user@your_server.beget.tech:~/public_html/

# 2. Подключитесь к серверу
ssh your_user@your_server.beget.tech

# 3. Распакуйте и настройте
cd public_html
tar -xzf mindheartsoul-dist.tar.gz
mv dist/* ./
rm -rf dist mindheartsoul-dist.tar.gz

# 4. Создайте .htaccess
cat > .htaccess << 'EOF'
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
EOF

# 5. Проверьте
ls -la
```

---

## 📊 Характеристики деплоя

- **Размер архива:** 118 KB (сжатый)
- **Размер распакованный:** ~470 KB
- **Тип:** Статический SPA (Single Page Application)
- **Требования:** Apache с mod_rewrite
- **PHP:** Не требуется
- **База данных:** Не требуется (все в LocalStorage)
- **SSL:** Опционально, но рекомендуется

---

## 🌐 URL после деплоя

После деплоя ваше приложение будет доступно по адресу:

- **HTTP:** `http://your-domain.beget.tech/`
- **HTTPS:** `https://your-domain.beget.tech/` (если настроен SSL)

**Замените** `your-domain.beget.tech` на ваш реальный домен в Beget Cloud.

---

## ✅ Checklist финального деплоя

- [ ] Архив `mindheartsoul-dist.tar.gz` загружен
- [ ] Содержимое распаковано в `public_html/`
- [ ] Файл `.htaccess` создан с правилами SPA
- [ ] Сайт открывается в браузере
- [ ] React Router работает (переходы по страницам)
- [ ] Обновление страницы (F5) не вызывает 404
- [ ] SSL сертификат настроен (опционально)
- [ ] Проверены все основные страницы

---

## 🎉 Готово!

После выполнения всех шагов ваше приложение **Mind Heart Soul** будет работать на Beget Cloud!

Все изменения, которые вы делаете локально, можно деплоить заново:
1. `npm run build`
2. Создать новый архив `dist/`
3. Загрузить на Beget
4. Распаковать (заменив старые файлы)

**Удачи с деплоем!** 🚀
